// Build-time constants injected by Vite's `define` config in
// vite.config.ts. These exist at runtime as string literals; the
// .d.ts here just tells TypeScript to allow them.
declare const __BUILD_DATE__: string;
